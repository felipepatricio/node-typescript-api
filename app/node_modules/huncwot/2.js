const generate = ({ name }) => `
export class ${name} implements I${name} {
  private hostname: string
  private fetch: Fetch
  private path = '/rpc/${name}/'
}
`;

console.log(generate({ name: 'User' }));
