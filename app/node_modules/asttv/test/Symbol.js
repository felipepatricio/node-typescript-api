const { equal } = require('assert')
const subject = require('..')

equal(subject({
  type: 'CallExpression',
  callee: {
    type: 'Identifier',
    name: 'Symbol'
  },
  arguments: [
    { type: 'Literal', value: 42 }
  ]
}).toString(), 'Symbol(42)')
